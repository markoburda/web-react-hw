import React from 'react';

import "../style/footer.css"

export default class Footer extends React.Component {
    render() {
        return (
            <div className="footer-container">
                    <p>Build with React App</p>
            </div>
        )
    }
}